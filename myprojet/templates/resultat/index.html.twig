{% extends 'crud_base.html.twig' %}

{% block title %}Résultats{% endblock %}

{% block body %}
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">Résultats</h1>
    <a class="btn btn-primary" href="{{ path('app_resultat_new') }}">Créer un résultat</a>
  </div>

  {% if resultats is empty %}
    <div class="alert alert-info">Aucun résultat.</div>
  {% else %}
    <div class="table-responsive">
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>ID</th>
            <th>Note</th>
            <th>Appréciation</th>
            <th>Élève ID</th>
            <th>Examen</th>
            <th class="text-end">Actions</th>
          </tr>
        </thead>
        <tbody>
          {% for resultat in resultats %}
            <tr>
              <td>{{ resultat.id }}</td>
              <td>{{ resultat.note }}</td>
              <td>{{ resultat.appreciation }}</td>
              <td>{{ resultat.eleveId }}</td>
              <td>{{ resultat.examen ? resultat.examen.titre : '' }}</td>
              <td class="text-end">
                <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_resultat_show', {'id': resultat.id}) }}">Voir</a>
                <a class="btn btn-sm btn-outline-primary" href="{{ path('app_resultat_edit', {'id': resultat.id}) }}">Éditer</a>
              </td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  {% endif %}
{% endblock %}
