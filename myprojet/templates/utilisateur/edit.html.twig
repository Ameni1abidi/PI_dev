{% extends 'admin_base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}
  <section class="panel full">
    <div class="admin-page-header">
      <h1 class="admin-title">Modifier l'utilisateur</h1>
      <div class="admin-actions">
        <a class="btn btn-outline" href="{{ path('app_utilisateur_index') }}">Retour</a>
      </div>
    </div>

    {{ include('utilisateur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="admin-actions">
      {{ include('utilisateur/_delete_form.html.twig') }}
    </div>
  </section>
{% endblock %}
